let height = 10;
let base = 4;
const perimeterOfRectangle = 2 * (base + height);

const areaOfRectangle = base * height;


console.log(`The perimeter of rectangle with base of ${base} and height of ${height} is ${perimeterOfRectangle}`);

console.log(`The area of rectangle with base of ${base} and height of ${height} is ${areaOfRectangle}`);