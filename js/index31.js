let firstSide = 10;
let secondSide = 20;
let thirdSide = 5;
const perimeterOfTriangle = firstSide + secondSide + thirdSide;

const result = `The perimeter of triangle with side length of ${firstSide}, ${secondSide} and ${thirdSide} is ${perimeterOfTriangle}`;

console.log(result);